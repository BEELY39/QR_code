# Contracts

No external API contracts or external service dependencies are introduced in this feature.
All processing for Wi-Fi payloads, logo Base64 encoding, and SVG generation happens purely client-side (Zero-Serveur architecture).
Internal interfaces are documented in `data-model.md`.
